love2d/build/
# Build artifacts, both of them caches in front of procedural code and both
# regenerated by tools/build_web.sh: the sound bank (2.2 MB of Ogg from
# tools/bake_audio.sh) and the tree mesh library (12.5 MB of vertex buffers
# from tools/bake_trees.sh). The game runs happily without either.
love2d/src/bake/
/.vercel
/node_modules

# Scratch: raw music masters land here; only the encoded assets/music ogg ships.
love2d/tmp/
